<h1>Categories</h1>
<hr>
<% if @categories.length > 0 %>
  <% @categories.each do |category| %>
    <div class="category">
      <h3><%= link_to category.name, "/categories/#{category.id}" %></h3>
    </div>
  <% end %>
<% else %>
  <h3>There are no saved categories.</h3>
<% end %>
