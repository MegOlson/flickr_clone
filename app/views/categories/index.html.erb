<h1>Categories</h1>

<% if @categories.any? %>
  <% @categories.each do |category| %>
    <div class="category">
      <h3><%= link_to category.name, category_path(category) %></h3>
    </div>
  <% end %>
<% else %>
  <h3>There are no saved categories.</h3>
<% end %>
